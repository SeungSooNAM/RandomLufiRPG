<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<comment />
	<entry key="searchuserlogin">
		SELECT
			A.CHAR_NUMBER
			FROM U.USER_LOGIN
			JOIN A.CHARACTER USING (CHAR_NUMBER)
	</entry>
	<entry key="searhcharsce">
		SELECT
			  OPT_STR
			, OPT_DEX
			, OPT_CHA
			FROM SCE_OPTION
			JOIN SCENARIO USING(SCE_NUM)
			WHERE SCE_NUM = ?
			AND OPT_NUM = ?
	</entry>

</properties>
